document.addEventListener('DOMContentLoaded',()=>{const buttons=[...document.querySelectorAll('[data-filter]')];const rows=[...document.querySelectorAll('.publication')];buttons.forEach(button=>button.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;rows.forEach(row=>{const text=row.textContent.toLowerCase();row.hidden=filter!=='all'&&!text.includes(filter.toLowerCase())})}));fetch(window.SITE_CONFIG.contentEndpoint).then(r=>{if(!r.ok)throw new Error('Content data unavailable');return r.json()}).then(data=>{document.documentElement.dataset.articleCount=String(data.length)}).catch(()=>{document.documentElement.dataset.articleCount='static-fallback'})});